# Student_project
This project uses Express.js to build a basic server with routing and a Student Record Management System. It allows users to perform CRUD operations on student data through RESTful API endpoints.
